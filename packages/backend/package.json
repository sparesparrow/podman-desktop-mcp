{
  "name": "@podman-desktop-mcp/backend",
  "version": "1.0.0",
  "private": true,
  "main": "dist/extension.js",
  "engines": {
    "podman-desktop": "^1.0.0"
  },
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@podman-desktop/api": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@podman-desktop-mcp/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.12",
    "vitest": "^1.2.2",
    "@types/node": "^20.11.16"
  }
}
